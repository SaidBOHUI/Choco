{% for page in top %}

        <li>					
            <a href="{{path('page_show',{'slug' : page.slug}) }}" title="{{page.title}}">
                <span>{{ page.title }}</span>
            </a>
        </li>	

{% endfor %}
